/**
 * Vector Service REST API Client (Stub)
 *
 * Vector operations are handled internally by the memory API
 */
export class VectorServiceClient {
    constructor(_baseUrl, _authManager) {
        // Vector operations are handled internally by the memory service
        // This is a stub for compatibility
    }
}
//# sourceMappingURL=vector.js.map